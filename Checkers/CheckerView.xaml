<UserControl x:Class="Checkers.CheckerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Checkers"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <ComboBox x:Name="CheckerAuswahl"     HorizontalAlignment="Left" VerticalAlignment="Top" Width="300" ItemsSource="{Binding CheckerAuswahl}" SelectedItem="{Binding SelectedChecker}"/>
        <TextBox Name="TextBox1"  HorizontalAlignment="Left" Height="122" Margin="0,0,0,0" TextWrapping="Wrap" Text="{Binding Text}" VerticalAlignment="Top" Width="300" />
        <TextBlock x:Name="TextBlock1" HorizontalAlignment="Left" Margin="0,0,0,0" TextWrapping="Wrap" Text="{Binding Path=TextBoxMessage,UpdateSourceTrigger=Default,Mode=TwoWay}" TextBlock.FontSize="100" TextBlock.TextAlignment="Center" VerticalAlignment="Top" Height="121" Width="300" Background="Beige" />
        <Button x:Name="CheckButton" Content="Button" HorizontalAlignment="Left" Margin="0,0,0,0" VerticalAlignment="Top" Width="300"/>

    </StackPanel>

</UserControl>
